# 会话签名密钥（用 `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` 生成）
SESSION_SECRET=""

# 生产数据库连接串（Neon 等 Postgres；本地 SQLite 阶段可留空）
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
